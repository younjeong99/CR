<h1>Listing articles</h1>

<table>
    <tr>
        <th>Title</th>
        <th>Text</th>
    </tr>
    
    <% @articles.each do |article| %>
    <tr>
        <td><%= article.title %></td>
        <td><%= article.content %></td>
        <td><%= link_to 'index', article_path(article) %></td>
    </tr>
    <% end %>
</table>

<%= link_to 'New article', new_article_path %>
